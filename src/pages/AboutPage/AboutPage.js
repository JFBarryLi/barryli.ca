import NavBar from 'components/NavBar';

const AboutPage = () => (
      display: 'block',
      width: '100%',
      height: '100%',
    }}>
      <NavBar />
    </div>
);

export default AboutPage;
